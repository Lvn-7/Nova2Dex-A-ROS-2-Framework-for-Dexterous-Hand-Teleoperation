# Windows third-party dependencies

Downloaded Qt files are intentionally excluded from Git.

Place Qt 6.8.2 for MSVC 2022 at:

```text
third_party/Qt/6.8.2/msvc2022_64/
```

See `../README_CN.md` for the `aqtinstall` and `windeployqt` commands.

